"""
config/

All configuration for the app lives here.

  config/paths.py      — shared path resolution
  config/settings.py   — global app settings (config/settings.json)
  config/devices.py    — per-device configuration (config/devices.json)
  config/constants.py  — all named values, tagged [TUNABLE] or [INTERNAL]
"""
