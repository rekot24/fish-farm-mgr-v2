"""
detection/

Screen reading via template matching.

  detection/result.py        — DetectResult data shape
  detection/template_bank.py — loads and manages reference images (copied from fish-farm-manager)
  detection/detector.py      — runs template matching against captured frames (copied from fish-farm-manager)
"""
