"""
bot/

Core worker logic for the app.

  bot/states.py         — state name constants
  bot/actions.py        — all tap/click/launch actions
  bot/device_worker.py  — main per-device loop
  bot/device_manager.py — manages all device workers
  bot/app_logger.py     — persistent logging (copied from fish-farm-manager)
"""
